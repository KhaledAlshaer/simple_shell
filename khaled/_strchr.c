#include "main.h"

/**
 * - This is the function
 *@: this is a variable
 *@: this is a variable
 *Return: returns 0
*/
char *_strchr(const char *str, int character)
{
	while (*str != '\0')
	{
		if (*str == character)
		{
		}
	}
}
